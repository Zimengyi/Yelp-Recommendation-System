# ============================================================
# Yelp Recommendation System — Python dependencies
# Tested with Python 3.10+ on macOS (M3 Pro) and Colab T4
# ============================================================

# Data
pandas>=2.1.0
numpy>=1.26.0
polars>=0.20.0          # fast aggregation for user feature precompute (see explainer §2.5)
pyarrow>=14.0.0         # parquet I/O

# Geo
haversine>=2.8.0        # mean_distance_traveled feature

# Recommender baselines
scikit-surprise>=1.1.3  # MF baseline (Surprise SVD)
# xlearn>=0.40           # FM baseline — install via conda if pip fails on macOS
# pyfm>=0.1              # FM fallback if xlearn fails

# Deep learning + DeepFM
torch>=2.1.0
deepctr-torch>=0.2.9    # DeepFM main model

# Embedding / NLP
sentence-transformers>=2.3.0  # activity_emb feature (32-dim)

# Two-Tower retrieval (Phase 6 stretch)
faiss-cpu>=1.7.4

# Evaluation + viz
scikit-learn>=1.4.0
matplotlib>=3.8.0
seaborn>=0.13.0
tqdm>=4.66.0

# LLM agent (Phase 8)
anthropic>=0.34.0

# Demo
streamlit>=1.30.0

# Dev
pytest>=8.0.0
ruff>=0.4.0             # lint
black>=24.0.0           # format
ipykernel>=6.29.0       # notebooks
